services:
  - type: web
    name: christmas-wish-app
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /
    routes:
      - path: /
        matchType: prefix
